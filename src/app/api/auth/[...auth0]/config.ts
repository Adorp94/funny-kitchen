// Using the edge runtime for middleware compatibility
export const runtime = 'edge';

// Configuration is managed by environment variables
// We're not using this file directly since Auth0 SDK v4 handles this internally
export default {
  // This is just a placeholder to avoid import errors
  version: "@auth0/nextjs-auth0 v4.3.0"
}; 